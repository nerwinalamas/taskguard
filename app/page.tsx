import TaskTable from "@/components/TaskTable";

const Home = () => {
    return (
        <main className="mt-20 flex items-start justify-center">
            <TaskTable />
        </main>
    );
};

export default Home;
